# IUCN Red List API v4 token.
# Request one at https://api.iucnredlist.org/users/sign_up
# Used server-side only (build script + /api/species route). Never shipped to the browser.
IUCN_TOKEN=your_iucn_api_v4_token_here
